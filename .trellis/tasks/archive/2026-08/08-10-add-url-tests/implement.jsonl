{"file": ".trellis/spec/shared/dependencies.md", "reason": "Vitest is added as a root workspace devDependency via `pnpm add -Dw vitest`; follow the pinned-major + workspace devDep conventions here so the image build (which skips devDeps) is unaffected."}
{"file": ".trellis/spec/frontend/deployment.md", "reason": "Single-image SQLite + supervisord + compose topology. The dev-only 8000:8000 publish goes in docker-compose.yml only; Dockerfile EXPOSE stays 3000 and docs/qnap-deployment.md is untouched."}
{"file": ".trellis/spec/backend/performance.md", "reason": "Sidecar /v1/fetch + /health HTTP contract, the detectBlockedPage blocked-signature registry, SPA render-wait behavior, and Akamai probabilism — exactly what the acceptance scenarios assert against."}
